-- CleanAUS Database Initialization Script
-- This file runs automatically on first PostgreSQL startup

-- Enable required extensions
CREATE EXTENSION IF NOT EXISTS "uuid-ossp";
CREATE EXTENSION IF NOT EXISTS "pg_trgm";

-- Note: Tables are created by TypeORM synchronization in development
-- or by migrations in production. This script only sets up extensions.
